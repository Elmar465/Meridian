spring:
  application:
    name: meridian

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/meridian}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:Elmar-465}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:elmar465@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:resbgpogpjkooxls}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  frontend-url: ${APP_FRONTEND_URL:https://meridian-front-end.vercel.app}

file:
  upload-dir: uploads

server:
  port: ${PORT:8081}

jwt:
  secret: ${JWT_SECRET:mni1T1FvYLls72sdkfBPlvkBRJTERKqOh15X7zuMoQ0=}
  expiration: 86400000

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  packages-to-scan: com.projectnova.meridian.controller

resend:
  api-key: ${RESEND_API_KEY:re_jB9xU72U_JutwRMHwcY7Y1uoNhfxpxXpp}